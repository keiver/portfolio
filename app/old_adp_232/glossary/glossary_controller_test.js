'use strict';

describe('adp.glossary module', function() {
  beforeEach(module('adp.blog'));
  describe('adp.glossary controller', function(){

    it('should ....', inject(function($controller) {
      //Test Body
      var glossary = $controller('glossaryController');
      expect(glossary).toBeDefined();
    }));

  });
});