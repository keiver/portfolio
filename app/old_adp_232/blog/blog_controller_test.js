'use strict';

describe('adp.blog module', function() {
  beforeEach(module('adp.blog'));
  describe('adp.blog controller', function(){

    it('should ....', inject(function($controller) {
      //Test Body
      var blog = $controller('blogController');
      expect(blog).toBeDefined();
    }));

  });
});