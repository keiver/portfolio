'use strict';

describe('adp.contact module', function() {
  beforeEach(module('adp.contact'));
  describe('adp.contact controller', function(){

    it('should ....', inject(function($controller) {
      //Test Body
      var blog = $controller('contactController');
      expect(blog).toBeDefined();
    }));

  });
});